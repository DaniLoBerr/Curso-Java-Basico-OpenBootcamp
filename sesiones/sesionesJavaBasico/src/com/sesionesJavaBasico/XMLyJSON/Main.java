package com.sesionesJavaBasico.XMLyJSON;

public class Main {

    /**
     * XML(XaMel) y JSON (JavaScript Object Notation)
     *
     * Son dos formatos de intercambio de información que se utilizan
     * para representar información pero de forma estructuradda.
     *
     * XML se utilizaba mucho antes y aún hoy en día hay mucho
     * dato escrito en él y JSON es algo más actual.
     * JSON es un tipo de dato nativo tanto para JavaScript
     * como para otros lenguajes (en Python, por ejemplo, son
     * como diccionarios), lo cual hace que no haya que realizar
     * conversiones a la hora de trabajar con ellos.
     *
     * XML está formado por etiquetas y atributos, JSON está
     * formado por campos y documentos (lo de dentro de las llaves).
     *
     * Los JSON pueden contener arrays (información entre corchetes
     * en vez de llaves).
     *
     * Parsear XML
     *
     * Parsear quiere decir obtener la información del archivo
     * XML por parte del programa.
     *
     *
     */
}
