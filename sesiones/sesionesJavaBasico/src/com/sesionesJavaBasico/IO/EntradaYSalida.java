package com.sesionesJavaBasico.IO;

public class EntradaYSalida {

    /**
     *
     * Un programa informático es un proceso que se ejecuta que lee datos por
     * una entrada y los transforma en otros datos por una salida.
     * En Java, la IO (Input/Output) se base en el concepto de flujo,
     * que es una secuencia ordenada de datos que tienen una fuente o flujo
     * de entrada o un destino o flujo de salida. La entrada y salida en Java se
     * implementa en el paquete java.io.
     * Los programas pueden recibir datos de muchas maneras: por teclado,
     * por ficheros de texto, recibiendo datos de internet, etc.
     * Las salidas también pueden ser de muchas maneras: imprimiendo por pantalla,
     * convirtiéndolos en otros datos, enviándolos por internet, etc.
     *
     */
}
