package com.sesionesJavaBasico.refactoring.magicNumbers;

public class Main {

    /**
     * MAGIC NUMBERS
     *
     * Técnica basada en la utilización de constantes. Cuando
     * tenemos un valor que se repite en diferentes lugares de
     * nuestro código y que va a ser siempre el mismo,
     * podemos declarar una constante con ese
     * valor y así utilizar la constante donde haga falta; y, si es
     * necesario cambiar su valor, se cambiaría en la declaración de la
     * constante y no en todos las partes del código donde se utilice
     * ese valor.
     *
     * El valor de una constante solo se puede variar en el lugar
     * de la declaración, después desde el código no se puede modificar.
     *
     * HARD CODE
     *
     * Se denomina Hard Code al código que utiliza datos inscrustados
     * directamente en vez de utilizar variables o obtener los datos
     * de fuentes externas (lo que acabamos de ver).
     *
     *
     * OPERADOR TERNARIO
     *
     * Otra forma de escribir la expresión If/Else.
     */
}
