package com.sesionesJavaBasico.refactoring.extractMethod;

public class Main {

    /**
     * EXTRAER A UN MÉTODO
     *
     * Como veíamos en la técnica de "Consolidar un condicional",
     * se puede extraer una parte del código de un método a otro método.
     *
     * Es útil por ejemplo a la hora de entender un código, si agru-
     * pamos partes del código que tengan relación en métodos a los
     * que luego nombrar y entender qué hace cada uno.
     *
     * Lo idoneo es nombrar los métodos exactamente por lo que hacen: si
     * un código es imprimir un código HTML pues printHTML, si lo que hace
     * es extraer unos datos pues getData, etc.
     *
     * TEST DRIVEN DEVELOPMENT
     *
     * Es una metodología de desarrollo que se basa en la creación
     * de tests que dicen los resultados que se tienen que obtener
     * del código y luego desarrollar dicho código en base a eso.
     * Primero el código se desarrolla lo más simple posible para
     * que cumpla con los tets y luego se refactoriza para ir
     * optimizándolo.
     *
     */
}
