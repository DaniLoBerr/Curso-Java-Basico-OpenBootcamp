public class Main {

    public static void main(String[] args) {

        // FUNCIONES

        hola

        /**
         * Si hacemos un refactor en una variable y le
         * cambiamos el nombre, este se cambiará en el resto
         * de sitios en los que hemos utilizado
         * la variable.
         */

        /**
         * Sobre el Debuger
         *
         * Con "step over" vamos depurando el código línea a línea
         * pero no entra en el código de las funciones, las va saltando.
         * Con "step into" entra paso a paso en las funciones.
         *
         */

        /**
         * En una cadena de texto, el código "/n" crea un salto de línea.
         */

        /**
         * Javadocs intellij idea: Para dudas sobre el IDE IntelliJ
         */

        /**
         * TIPOS FUNCIONES:
         *
         * 1 - Sin parámetros y sin devolver datos
         * 2 - Sin parámetros y devolviendo datos
         * 3 - Con parámetros y sin devolver datos
         * 4 - Con parámetros y devolviendo datos
         */

        /**
         * SOBRECARGA DE FUNCIONES:
         *
         * Funciones iguales pero que a las que se les
         * da diferente número de parámetros o los mismos
         * parámetros pero con diferente tipo de dato
         */
    }
}
