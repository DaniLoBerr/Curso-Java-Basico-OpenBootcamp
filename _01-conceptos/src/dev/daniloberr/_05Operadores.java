package dev.daniloberr;

public class _05Operadores {

    public static void main(String[] args) {

        // Tipos de operadores

            // Aritméticos

                int number1 = 5;
                int number2 = 2;

                int resultado1 = number1 + number2; // Suma
                int resultado2 = number1 - number2; // Resta
                int resultado3 = number1 * number2; // Multiplicación
                int resultado4 = number1 / number2; // División
                int resultado5 = number1 % number2; // Resto
                    /*
                      En la división hay que tener en cuenta el tipo de dato que ponemos porque si
                      definimos la variable como un int y el resultado de la división tiene decimales, estos
                      se pierden.
                    */
                /*
                  También ocurre que a la variable resultado tenemos que darle diferentes nombres porque
                  no podemos definir una variable con un mismo nombre dentro de una clase.

                  Lo que si se puede hacer es lo siguiente:

                    int resultado = number1 + number2;
                    resultado = number1 - number2;

                  Aquí lo que estaríamos haciendo sería sobreescribir el valor de la variable.
                 */

            // Lógicos, relación, comparación, booleanos

                /*
                    // > Mayor que
                    // >= Mayor o igual que
                    // < Menor que
                    // <= Menor o igual que
                    // == Igual que
                    // != Distinto que
                    // && Condición and
                    // || Condición or
                    // ! Negación (!true = not true = false)
                 */

            // Asignación

                /*
                    // = Igual
                    // += Más igual
                    // -= Menos igual
                    // *= Multiplicación igual
                    // /= División igual
                    // &= Resto igual
                 */

            // Incremento

                // ++

            // Decremenento

                // --

            // Concatenación

                // + (Orientado a concatenar cadenas de texto)
    }

}
